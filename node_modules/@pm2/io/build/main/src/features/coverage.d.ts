import { Feature } from './featureTypes';
export default class CoverageFeature implements Feature {
    private inspectorService;
    private method;
    constructor();
    init(): Promise<{}>;
    destroy(): void;
    start(opts: any): Promise<{}>;
    stop(): Promise<string>;
    private getInfo;
}
